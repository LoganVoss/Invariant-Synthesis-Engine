# Examples

- `energy_grid_demo.py` recovers a planted scale-invariant oscillatory growth coordinate
  from matched-statistics event windows.
- `custom_system.py` is a minimal adaptation template for fixed-window telemetry.

Run from the repository root after `pip install -e .`:

```bash
python examples/energy_grid_demo.py
```
